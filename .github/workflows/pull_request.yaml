name: pull_request

on: [pull_request]

jobs:
  pull_request:
    uses: MapColonies/shared-workflows/.github/workflows/pull_request.yaml@v2
    secrets: inherit
    with:
      openApiFilePath: './bundledApi.yaml'

  helm_lint:
      uses: MapColonies/shared-workflows/.github/workflows/helm-lint.yaml@chore/add-chart-lint
      secrets: inherit
